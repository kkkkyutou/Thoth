# @thoth/tui Tests

Future home for OpenTUI rendering, input and workspace-flow tests.

Current status:

1. Unit tests currently live beside the TypeScript source under `src/*.test.ts`.
2. Existing tests cover the pure TUI surface model and the OpenTUI runtime guard.
3. Native renderer tests are deferred until the Node FFI vs Bun runtime spike is explicitly resolved.
