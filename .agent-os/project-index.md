# Project Index

## Current Truth

1. Objective: `NTH-OBJ-001`
2. Top next action: `NTH-TD-017`
3. Active workstreams: `NTH-WS-001`, `NTH-WS-002`, `NTH-WS-003`, `NTH-WS-004`, `NTH-WS-005`, `NTH-WS-006`
4. Active blockers: None for the current top next action. `NTH-TD-016` / `NTH-MS-013` passed the reopened Loop-2 reset under `NTH-EV-029`; provider-backed `thoth.loop` PlanExec / Review remains intentionally deferred to `NTH-TD-019`.
5. Current branch: `agent/dev/mvp`
6. Current implementation state: promoted upstream-derived implementation substrate in formal `packages/*` source trees with first-day foundation infrastructure; Thoth/Paseo parallel runtime isolation is verified with Thoth daemon on `127.0.0.1:6688`, local Paseo daemon preserved on `127.0.0.1:6767`, real web review on `8082 -> 8148`, independent test relay deployment at `relay.test.thoth.seeles.ai`, Linux AppImage, Android Debug APK and Codex provider smoke. Revised Loop Goal 1 `NTH-TD-015` is verified by `NTH-EV-025` for the pre-runtime-tool-bridge Clarify skill/golden contract. Loop Goal 2 `NTH-TD-016` / `NTH-MS-013` is verified by `NTH-EV-029`: the restored Paseo production app surface is again the main path; `Quick + none` is a bare Codex/Paseo foreground stream with no Clarify card/packet/repair; `Quick + Dive` uses Codex app-server `dynamicTools` / `item/tool/call` semantic Thoth runtime tools, persisted pending decisions and AgentTimeline rendering; Clarify cards submit to readonly folded summaries and continue provider timeline; Task Card is a compact CEO overview; Pyramid Plan Card is hierarchical; Quick approvals continue same-session `quick_exec` with real shell/edit timeline; Loop approvals register durable `registered_pending` tasks without fake PlanExec/Review. Evidence includes public `8148` and local `8082` real Codex journeys, desktop/mobile screenshots opened with `view_image`, Background Tasks list/detail recovery, mobile deep-link recovery, independent `codex exec` UI mental-model review PASS, focused protocol/client/daemon/app tests, `npm --workspace=@thoth/app run test`, `npm run build:web`, `npm run check:foundation` and `git diff --check`. Provider-backed `thoth.loop` PlanExec / Review runtime remains open for `NTH-TD-019`, not covered by Loop-2.

## Objective Summary

`NTH-OBJ-001`: Build Thoth as a local-first AI task control plane that reduces user cognitive burden and entry barrier, compiles vague intent into clear tasks, runs recoverable asynchronous loops, verifies results with evidence, and remains host-neutral across future harness tools.

## Active Workstreams

1. `NTH-WS-001` `[active]`: Preserve Thoth product authority and decision boundaries.
2. `NTH-WS-002` `[active]`: Establish the TypeScript / Node monorepo skeleton.
3. `NTH-WS-003` `[active]`: Design the clarify-to-task and aggressive loop runtime before implementation.
4. `NTH-WS-004` `[active]`: Keep harness, relay, mobile, desktop, TUI and ACP references current enough for implementation.
5. `NTH-WS-005` `[active]`: Maintain packaging and release infrastructure without publishing.
6. `NTH-WS-006` `[active]`: Preserve upstream provenance while converting imported material into Thoth formal source.

## Top Next Action

`NTH-TD-017` `[ready]`: Start Loop Goal 3, the backend Task Contract Compiler and Approval Harness, now that Loop-2's restored Paseo surface, Codex runtime-tool Clarify bridge, AgentTimeline cards, Quick+none bare stream, Quick+Dive same-session `quick_exec`, Loop `registered_pending`, recovery evidence and gates are verified under `NTH-EV-029`.

## Active Blockers

None. `NTH-TD-017` is unblocked by `NTH-EV-029`; `NTH-TD-019` remains a future scope item, not a blocker for the current top next action.

## Recent Important Changes

1. `NTH-CD-001`: User decided Thoth will not continue archived plugin compatibility. The archived plugin line is archived at release `thoth-plugin-final-archive` and branch `archive/main-20260627`.
2. `NTH-CD-002`: User approved a full repo reset on `port/from-archived-plugin`: delete archived Python/plugin runtime, keep only design authority and a TypeScript / Node monorepo skeleton.
3. `NTH-CD-003`: User initially selected `GPL-3.0-only`, `npm workspaces`, package namespace `@thoth/*`, and OpenTUI as the fixed TUI framework; the license portion is superseded by `NTH-CD-017`.
4. `NTH-CD-004`: User selected docs-only prompt seed extraction. Archived prompt assets are summarized in `.agent-os/designs/thoth-prompt-contract-seeds.md`, not retained as Python code.
5. `NTH-CD-012`: User clarified that Thoth is not a harness tool or hidden LLM API wrapper. All AI and agent execution must come from configured provider sessions through ACP, harness runtime, app-server, official harness SDK/control surface or local harness CLI.
6. `NTH-CD-013`: User clarified that zero-shot semantic routing cannot be local deterministic logic. Task mode should be explicit in the composer or judged by a provider-backed session; local code can only enforce explicit controls and mechanical authority checks.
7. `NTH-CD-014`: User locked the chatbox controls as `+`, Provider, Mode, Clarify and Loop.
8. `NTH-CD-015`: User updated the business flow: `Quick + Don't Bother Me` is provider passthrough; `Loop` is read-only Clarify -> frozen contract -> one PlanExec provider session using provider-native plan mode when available -> independent Review. PlanExec clarification questions after freeze are auto-answered from the contract or first recommendation, but permission requests still follow permission policy. User-facing labels are `Don't Bother Me`, `One Plan, One Do` and `Run Until Stopped`.
9. `NTH-CD-017`: User selected AGPL for Thoth, upstream implementation seed import from the latest GitHub source, no Multica source copy, ignored raw cache under `.agent-os/upstreams/`, tracked broken `_paseo/` seeds and no voice/audio/speech/dictation material.
10. `NTH-EV-002`: AGPL and upstream seed import verification passed for metadata, ignored raw cache, seed directories, path exclusions, large-file hygiene, secret-like scan, package-lock refresh and `git diff --check`.
11. `NTH-CD-018`: User approved promoting all tracked `_paseo` implementation seeds into formal package source trees, deleting `_paseo`, preserving the 10 package boundary, keeping `packages/app/highlight` nested, refreshing the lockfile, and accepting expected broken compile.
12. `NTH-EV-003`: `_paseo` promotion verification passed for structure, metadata, lockfile refresh, ignored raw cache, package/config voice-residue scan, generated/cache scan, package-boundary scan, secret-like scan and `git diff --check`.
13. `NTH-CD-019`: First-day development infrastructure is locked: Node `24.14.0`, npm `11.9.0`, root-script gates, `oxfmt`/`oxlint`, foundation packages, stable install policy, local Android Debug APK, Linux-safe iOS scripts, package AGENTS contracts and no push/publish/GitHub Actions in this round.
14. `NTH-EV-004`: First-day development infrastructure verification passed: `npm install`, `npm run check:foundation`, Android doctor/toolchain/APK, iOS Linux behavior, AGENTS symlink coverage, generated path hygiene and `git diff --check`.
15. `NTH-CD-020`: Thoth I dev UI must be the same user experience as the current releasable full UI. Humans use it for dogfood/review; agents use standard repository tests and gates for code validation.
16. `NTH-CD-021`: Relay is v3-only with daemon-first room registration and role-scoped subprotocol tokens; local web preview and 200-client local relay load test passed, while the earlier Code4Agent mirror deployment path was blocked by protected paths.
17. `NTH-CD-022`: Runtime isolation is locked and verified: Thoth uses `127.0.0.1:6688`, local Paseo/legacy keeps `127.0.0.1:6767`, human web review uses `8082 -> 8148`, relay test service is deployed from independent `SeeleAI/Thoth-Relay`, and local desktop/mobile artifacts are produced without becoming releases.
18. `NTH-EV-006`: Thoth/Paseo isolation verification passed for daemon ports, web smoke, relay health/action/load test, Codex provider smoke, desktop AppImage smoke, Android Debug APK permission/package identity, foundation gate and git hygiene.
19. `NTH-EV-007`: Web workspace white-screen regression is fixed. Workspace navigation and `hi` submission no longer crash the browser; the route now surfaces the expected current `Select model` validation.
20. `NTH-CD-024`: Thoth UI icon system is locked to the final single-version `05-arcade-inventory` set under `packages/app/assets/icons/arcade-inventory/`.
21. `NTH-CD-025`: UI development branch is renamed from `port/from-archived-plugin` to `agent/dev/ui` before pushing the current UI shell/icon/runtime work.
22. `NTH-EV-008`: One Thoth Web/Desktop shared shell slice passed for the current scope. The real open-project entry now uses the locked transparent arcade-inventory PNG icon set, presents One Thoth / task control plane product language, shows honest workspace/provider/relay/review states, and preserves existing Add project, Import session, Provider setup and Pair device flows. This does not complete OpenTUI or the full task/Clarify/Loop UI productization goal.
23. `NTH-EV-009`: Workspace composer/task surface slice passed for the current scope. The web bundle now contains fixed Thoth-level composer controls for `+`, Provider, Mode, Clarify and Loop, honest preview/needs-provider states, Workspace active-task/contract/evidence slots and the MVP `10MB` attachment limit. This does not implement provider-backed Router, Clarify runtime, contract freeze, PlanExec, Review or OpenTUI.
24. `NTH-EV-010`: OpenTUI shell surface foundation passed for the current scope. `packages/tui` now derives Home, Workspace, Task / Loop, Providers, Connections, Evidence / Review and Settings / About slots from shared client/protocol shapes, has a guarded `@opentui/core` renderer factory, and truthfully reports the locked Node `24.14.0` runtime as unable to create the native renderer without Node `26.3.0+` plus experimental FFI or a future Bun decision. This does not complete the interactive native TUI app or the Node FFI vs Bun runtime spike.
25. `NTH-EV-011`: OpenTUI renderer smoke now has a reproducible root-script path. `npm run smoke:tui:renderer` builds `@thoth/tui`, runs `@opentui/core/testing` under pinned `node-linux-x64@26.4.0 --experimental-ffi`, renders the One Thoth TUI surface, captures a character frame and verifies the Home/Workspace/Task/Providers/Connections/Evidence/Settings/composer/authority text. Narrow `72x34`, default `96x34` and wide `132x34` smoke runs passed. This does not complete the interactive CLI workspace TUI or daemon-connected TUI flow.
26. `NTH-EV-012`: OpenTUI interaction navigation slice passed for the current scope. `packages/tui` now has a pure interaction state layer for focus, route history and explicit composer controls, the renderer shows route/focus/state/composer values and authority guard text, and `npm run smoke:tui:navigation` verifies a simulated Evidence / Review + Loop control frame under pinned Node `26.4.0 --experimental-ffi` at default `96x34`, compact `72x34` and wide `132x34`. This does not complete a live CLI TUI, native keypress loop or daemon-connected workspace TUI.
27. `NTH-EV-013`: OpenTUI CLI dogfood entry passed for the current scope. Top-level `thoth tui` now connects through existing CLI daemon utilities, reads real workspace/agent/provider snapshots, chooses the most specific registered workspace for the current `pwd`, mounts the shared OpenTUI surface and supports deterministic key-driven focus/route/composer controls. `npm run smoke:tui:cli` passed against Thoth `127.0.0.1:6688` under pinned Node `26.4.0 --experimental-ffi`, with compact `72x34` and wide `132x34` CLI smokes also passing. This still does not complete final OpenTUI onboarding, live refresh, task backend or full UI scorecard.
28. `NTH-EV-014`: OpenTUI live refresh/recovery slice passed for the current scope. `thoth tui` now exposes `R refresh`, re-fetches real daemon workspaces/agents/provider snapshots from the CLI/client layer, displays `Snapshot` / updated-at state, keeps disconnected hosts inside an honest recovery surface and asserts final frames do not contain relay pairing token material or legacy `6767` fallback text. `npm run smoke:tui:cli:recovery` now verifies the unreachable-host recovery frame. This still does not complete final OpenTUI onboarding, in-TUI workspace registration, richer route detail panels, task backend or full UI scorecard.
29. `NTH-EV-015`: OpenTUI route detail panels passed for the current scope. Home, Workspace, Task / Loop, Providers, Connections, Evidence / Review and Settings / About now expose route-specific detail sections derived from daemon/client/provider/agent/refresh inputs, while preserving honest preview/unavailable/needs-provider/needs-workspace states and avoiding TUI-owned durable task authority. Renderer, navigation, connected CLI, recovery CLI, compact and wide CLI smokes passed under the pinned Node `26.4.0 --experimental-ffi` path. This still does not complete final OpenTUI onboarding, in-TUI workspace registration, task backend, Clarify/Loop/Review runtime, long PTY stress or full UI scorecard.
30. `NTH-EV-016`: OpenTUI onboarding next-actions and current-`pwd` workspace registration passed for the current scope. `packages/tui` now derives `Next Actions` for workspace/provider/relay/recovery states, `W` registers the current `pwd` through the real daemon `workspace.create.request`, and TUI no longer falls back to an unrelated workspace when launched from an unregistered directory. A new smoke registers a temporary directory, verifies the final Workspace frame, archives the temporary workspace and confirms no `/tmp/tmp.*` active workspace remains. This still does not complete provider setup editing, relay pairing execution inside TUI, task backend, Clarify/Loop/Review runtime, full PTY stress or the full UI scorecard.
31. `NTH-EV-017`: OpenTUI provider readiness action passed for the current scope. `P providers` now routes through the CLI/client layer to call the real daemon `refreshProvidersSnapshot({ cwd })`, then reloads the Providers route from daemon snapshot state with honest readiness copy. A new smoke verifies the real `thoth tui` provider setup path under pinned Node `26.4.0 --experimental-ffi`, rejects fake provider configuration and sensitive relay/legacy host leakage, and compact `72x34` renderer smoke keeps both `W` and `P` next actions visible. This still does not complete provider/model editing or auth setup inside TUI, relay pairing execution inside TUI, task backend, Clarify/Loop/Review runtime, long PTY stress or the full UI scorecard.
32. `NTH-EV-018`: OpenTUI device pairing action passed for the current scope. `D devices` now routes through the CLI/client layer to call the real daemon `getDaemonPairingOffer({ timeout: 5000 })`, parses the sensitive offer only in CLI code, then reloads the Connections route with safe endpoint/expiry summary and credential-safety copy. A new smoke verifies the real `thoth tui` pairing path under pinned Node `26.4.0 --experimental-ffi`, rejects raw offer URLs, QR text, pairing tokens, relay subprotocol tokens and legacy `6767` hosts, and confirms temp workspace cleanup remains clean after regression smokes. This still does not complete full paired-device persistence UI, provider/model editing or auth setup inside TUI, task backend, Clarify/Loop/Review runtime, long PTY stress or the full UI scorecard.
33. `NTH-EV-019`: Desktop semantic menu and working UI scorecard baseline passed for the current scope. Desktop top-level menus now use `Thoth`, `File`, `Workspace`, `Task`, `Provider`, `View`, `Window` and `Help`, with unfinished Workspace/Task/Provider product actions disabled rather than faked. `docs/ui-review-scorecard.md` now records the final Web/Desktop/OpenTUI scoring rubric, current failing working scores and missing screenshot/stress evidence. This still does not complete final UI acceptance or the required >90 overall / OpenTUI >=88 score threshold.
34. `NTH-EV-020`: OpenTUI pseudo-terminal stress passed for the current scope. `npm run smoke:tui:pty-stress` builds the real TUI/CLI path, runs `thoth tui` under `/usr/bin/script` with pinned Node `26.4.0 --experimental-ffi`, exercises route/focus/composer/refresh/provider/device churn at `72x34`, `96x34` and `132x34`, verifies final Connections offer-ready frames and rejects raw offer/token/QR/legacy-host leakage. The UI scorecard now raises OpenTUI's working score to `87`, still below the final `88` threshold.
35. `NTH-EV-021`: Web static export scorecard smoke passed for the current scope. `npm run smoke:web:ui-scorecard` builds the real web export, serves `packages/app/dist`, runs Playwright against `E2E_BASE_URL`, captures Home desktop/mobile, Workspace composer and Settings About/Providers/Connections screenshots, and stress-tests route/composer/settings churn. The UI scorecard now raises Web's working score to `80` and overall to `80`, still below final acceptance.
36. `NTH-CD-027`: User rejected the Paseo-like APP shell direction and locked the compact APP model: Settings, Workspace Secretary and Background Tasks only; no General Chat view. Workspace `New Agent` remains as a new secretary topic/session. Quick stays foreground; Loop registers a background task only after task overview and goal contract confirmations. Hidden built-in `thoth.clarify` and `thoth.loop` runtime skills plus compact packets/state codes are mandatory.
37. `NTH-EV-022`: Compact APP runtime contract passed for the current scope. `.agent-os/designs/thoth-app-runtime-contract.md` is now canonical design authority, `packages/protocol/src/thoth-runtime-contract.ts` is protocol code authority, and targeted protocol test/typecheck/build passed. This does not implement the runtime or APP.
38. `NTH-CD-028`: Active development branch is renamed from `agent/dev/ui` to `agent/dev/mvp`, and the current branch goal is upgraded from UI-only implementation to MVP implementation.
39. `NTH-CD-029`: MVP implementation is decomposed into six independently executable Codex loop goals, alternating backend and frontend.
40. `NTH-CD-030`: The six loop goals are reframed around agent harness and agent engineering rather than daemon-first mechanics. Active sequence: `NTH-TD-015` Clarify Agent Harness + Convergence Contract, `NTH-TD-016` Secretary Clarify Experience, `NTH-TD-017` Task Contract Compiler + Approval Harness, `NTH-TD-018` Task / Goal Approval Experience, `NTH-TD-019` Loop Execution + Review Agent Harness and `NTH-TD-020` Background Task Dogfood Experience.
41. `NTH-CD-031`: `thoth.clarify` and `thoth.loop` development must be golden-driven and independently judged. Golden data must capture expected agent behavior, and an independent `codex exec` judge must review transcripts for behavioral psychology, behavior-tree convergence, frozen goals/constraints/acceptance and low user cognitive load before skill behavior is accepted.
42. `NTH-CD-032`: Final Clarify confirmation packets must preserve provenance mechanically. `C_TASK_CARD` carries the full prior Clarify Q&A transcript verbatim. `C_GOAL_CARD` carries that transcript plus the exact user-approved CEO Task Card from the first confirmation round, and the goal split must be grounded in both.
43. `NTH-CD-033`: Loop 1 and Loop 2 Clarify behavior now centers on behavior-tree branch questions, not default recommendations. Clarify must preserve the user's stated target, reject downgrade fallback questions, answer `hi` simply and warmly, and use titled question cards with 2-4 branch choices plus optional notes. Paseo is only a reference for question-card rendering capability, not the Clarify mental model. The older per-round compact-preset wording is superseded by `NTH-CD-034`.
44. `NTH-CD-034`: Loop 1 revised `thoth.clarify` / `thoth.loop` into standard cross-provider internal Skill artifacts. `SKILL.md` is canonical, not TS prompt constants; internal skills do not install into `~/.codex/skills`, `~/.claude/skills` or `~/.agents/skills`; normal packets carry runtime data only; transition/session/repair markers carry `skill_ref` only when needed; repair is shape/state/provenance-only; independent `codex exec` user simulation is required.
45. `NTH-EV-024`: Revised Backend Clarify Agent Harness and Convergence Contract passed. `packages/drivers` now provides standard `SKILL.md` artifacts, session-scoped mount/no-global-pollution checks, compact turn/transition/repair packets, 15 deterministic behavior golden scenarios plus 9 revised skill/session/packet/user-simulation eval checks, upgraded independent golden judge and independent user-simulation judge evidence. This does not implement frontend Workspace Secretary rendering, live daemon provider-session orchestration, real background task registration or `thoth.loop` execution/review runtime.
46. `NTH-CD-035`: Clarify strength now has explicit behavior semantics (`none`, `light`, `balanced`, `dive`), assumption owners are explicit, normal packets carry controls/effective strength plus assumption/frontier refs, transition packets may carry `controls_changed`, and `C_ASK` now uses one titled card with 2-4 tightly related questions plus hidden internal meta.
47. `NTH-EV-025`: Loop-1 Clarify strength and decision-frontier revision passed. Deterministic eval now has 21 scenarios, proves `none` / `light` / `balanced` / `dive` behavior differences, validates multi-question cards and internal meta, and both independent `codex exec` golden and user-simulation judges passed with new evidence artifacts.
48. `NTH-CD-036`: Loop-2 was upgraded into a hard APP frontend refactor gate around the Thoth three-view Workspace Secretary product model. Its three-view main-path direction is now superseded by `NTH-CD-041`, while the hard acceptance posture, authority-boundary review and no-fake/no-parsing constraints remain binding where they do not conflict.
49. `NTH-CD-037`: Project naming is unified as Thoth. Docs, code comments, package descriptions, preset prompts and canonical design filenames use one Thoth product identity; historical implementation material is the archived plugin runtime, not another Thoth product identity.
50. `NTH-CD-038`: Loop-2 must validate against the real Seele relay test service `relay.test.thoth.seeles.ai`; fake links, localhost relay stand-ins, mock connected states and token-leaking relay evidence cannot satisfy acceptance.
51. `NTH-EV-026`: Loop-2 frontend APP refactor evidence is historical-only after `NTH-CD-039`. It proves the three-view APP shell, typed clean UI model boundary, relay-safe Settings state and independent UI mental-model review, but it no longer verifies Loop-2 because Quick, `hi`, Clarify and repair must now come from the configured real provider session.
52. `NTH-CD-039`: Workspace Secretary / Clarify / Loop acceptance is provider-backed only. Deterministic daemon greetings, fixed Quick replies, local Clarify card factories, offline acceptance fixtures, provider waterfall, assistant markdown JSON extraction and first-option fallback are all forbidden in production authority.
53. `NTH-CD-040`: Loop-2 frontend authority is provider-backed streaming-first with atomic decision cards. Ordinary Quick / `C_DIRECT` replies and progress stream as clean UI events; Clarify / Task / Goal cards render only after complete provider output and daemon validation; no local fallback, text parsing, fake provider/card/relay or non-atomic card path can satisfy production or acceptance.
54. `NTH-EV-028`: Loop-2 provider-backed streaming and atomic-card verification passed for the now-rejected three-view Workspace Secretary shell. After `NTH-CD-041`, this is historical evidence only: it proves useful provider/relay/stream/card machinery existed, but it no longer verifies the current Paseo-surface Loop-2 target.
55. `NTH-CD-041`: User superseded the Loop-2 frontend main path. Loop-2 must return to the original production-grade Paseo app surface and layout, preserve Paseo stream/timeline/composer/card/settings/host-provider/attachments/file-link/terminal/browser/file-pane/responsive/e2e capabilities, remove or isolate the Thoth toy shell, remap composer controls to Provider / Clarify / Mode, and connect Thoth runtime authority into restored Paseo without losing the original AgentTimeline substrate.
56. `NTH-CD-042`: User refined runtime phase semantics: `Quick + none` is bare provider stream with no Clarify injection/packet/repair; `Quick + clarify` uses phase-aware structured turns and same-session `quick_exec`; `Loop` uses secretary Clarify plus two approvals before registered background task handoff.
57. `NTH-CD-043`: Loop-2 Clarify bridge is now Codex app-server `dynamicTools` / `item/tool/call` with semantic Thoth tools, persisted pending authority decisions and AgentTimeline cards. `submit_clarify_packet`, native `outputSchema` packet and assistant markdown JSON are legacy/internal compatibility only, not Loop-2 acceptance paths; Loop-2 stops honestly at `registered_pending`.

## Read Next

1. [requirements.md](requirements.md)
2. [architecture-milestones.md](architecture-milestones.md)
3. [todo.md](todo.md)
4. [run-log.md](run-log.md)
5. [designs/最核心的设计理念.md](designs/最核心的设计理念.md)
6. [designs/thoth-app-runtime-contract.md](designs/thoth-app-runtime-contract.md)
7. [designs/thoth-mvp-loop-goals.md](designs/thoth-mvp-loop-goals.md)
